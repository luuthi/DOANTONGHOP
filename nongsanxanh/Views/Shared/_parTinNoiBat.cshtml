@using Bussiness.ViewModel;
@using Bussiness.Implement;
@using NSX_Common;

<div class="hot-news">
    <h4 class="title-hot">Tin mới</h4>
    <ul class="list-hot-news">
        @{
            List<NewsViewModel> lst = new List<NewsViewModel>();
            NewsService _iNewsService = new NewsService();
            lst = _iNewsService.GetAllNews().Where(m => m.Category.Contains(CategoryNew.Outstanding.ToString())).ToList();
            foreach (var item in lst)
            {
                <li class="clearfix">
                    <a href="/TinTuc/@Extentions.ToUnsignLinkString(@item.NameGroup)/@item.Id" class="col-sm-4">
                        <img src="~/Images/images/tin-tuc/@item.Image">
                    </a>
                    <a href="" class="col-sm-8">@item.Title</a>
                </li>
            }
        }
    </ul>
</div>
